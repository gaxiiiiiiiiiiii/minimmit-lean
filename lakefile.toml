name = "minimmit-lean"
defaultTargets = ["Mine"]

[[require]]
name = "mathlib"
scope = "leanprover-community"
rev = "v4.29.1"        # lean-toolchain (leanprover/lean4:v4.29.1) と一致させる

[[lean_lib]]
name = "Mine"
# Mine/ 配下の全ファイルをビルドする（root module の import 有無によらず）
globs = ["Mine", "Mine.+"]
